using UnityEngine;

public class AnimFinishRelay : MonoBehaviour
{
    public bool Finished;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public string outcome;
}
